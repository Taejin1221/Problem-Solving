// Codeforces_.cpp
#include <iostream>

using namespace std;

void solveTestCase( ) {
	
}

int main(void) {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);

	int t;
	cin >> t;
	while ( t-- ) {
		solveTestCase( );
	}

	return 0;
}
